# 2026-07-22 — URDF 导出与代码框架完善

## 做了什么
- SolidWorks URDF Exporter v1.6.1 下载并安装
- 使用插件从 SolidWorks 装配体（仿真模型.SLDASM）成功导出 URDF
- 导出文件归档到桌面 `arm_model/model.SLDASM/`：
  - URDF 模型（含 7 个 revolute + 1 个 continuous joint，mimic 约束正确）
  - 8 个 STL 网格（总计 ~400 KB，全部可入 Git）
  - launch 文件（display + gazebo）
- 复制 URDF + 网格 + launch 到 `arm-mod/urdf/`（6 个 STL 成功，Link_6/7 被 SolidWorks 占用待重试）
- 创建 `机械臂改装调试/Linux_Cline_交接文档.md` 一份给未来 Linux 环境 Cline 的交接文档
- 与 Claude 讨论并确认：全链路 100% Python 可行（pyserial+PyQt5+numpy+pyftdi），不需要 C++
- 确认 arm_model 的 URDF 导出正确（关节原点偏移、轴方向、限位均与 MATLAB 模型一致）

## 关键决策
- 采用 SolidWorks URDF Exporter 直接导出，而非手写 URDF——STL 体积小（<400KB），Git 同步完全可行
- 确定三条线分工：基础框架（我）+ 算法训练（学长）+ 探路平台（双方共用）
- 未来代码实现层面确定 Python 全链路、不使用 C++

## 踩坑/教训
- 第一版导出（Desktop/model）的 STL 体积巨大（两个 socket 各 28MB），因为用了之前 MATLAB 验证用的旧 STL 文件
- 第二版导出（Desktop/arm_model）用了装配体内部的 SLDPRT 源文件导出，体积 ~400KB
- Link_6.STL 和 Link_7.STL 被 SolidWorks 文件锁占用，需关闭 SolidWorks 后重新复制

## 下一步
- 关闭 SolidWorks 后复制 Link_6.STL 和 Link_7.STL 到 arm-mod
- 在 RViz 中验证 URDF 加载和关节运动
- 实现 kinematics.py 正运动学 fkine()
- 创建 ros2_ws 的 arm_description package